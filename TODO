Pino : logger

Entrées/Sorties : std(in/out), twitch, discord, youtube live
généralisation entrée/sortie => BotIO (BotInput + BotOutput)

CommandHandler
interface IBotCommand (supports() handle():CommandResponse)
- TwitterListBotCommand implement IBotCommand

BotIO::write(CommandResponse);

Cache avec Redis ? (ex : liste des 1000 utilisateurs machin)
